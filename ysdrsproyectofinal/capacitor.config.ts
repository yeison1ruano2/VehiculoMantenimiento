import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yeisonruano.proyectofinal',
  appName: 'proyectofinal',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
